server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: obione-backend

  datasource:
    url: jdbc:h2:mem:obione_dev
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true